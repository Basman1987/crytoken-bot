{
  "name": "crytoken-bot",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "pages:deploy": "wrangler pages deploy ./src",
    "dev": "wrangler pages dev ./src",
    "start": "wrangler dev"
  },
  "dependencies": {
    "ethers": "^6.11.1"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240208.0",
    "typescript": "^5.3.3",
    "wrangler": "^3.28.1"
  }
}

